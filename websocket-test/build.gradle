archivesBaseName = 'websocket-test'

//各种依赖的jar包
dependencies {
    implementation ('org.springframework.boot:spring-boot-starter-websocket')
    implementation "org.springframework.boot:spring-boot-starter-actuator"
}